// 搜索转圈
@keyframes x-rotate {
	from {
		transform: rotate(0deg);
	}

	to {
		transform: rotate(360deg);
	}
}

// UPGRATE:icon类名应为本地
.icon-x-loading:before {
	display: inline-block;
	animation: x-rotate 1s linear infinite;
}
